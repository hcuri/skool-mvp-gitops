apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: skool-mvp-web
  namespace: argocd
  labels:
    app: skool-mvp-web
spec:
  project: default
  sources:
    - repoURL: https://github.com/hcuri/skool-mvp-web.git
      targetRevision: main
      path: charts/skool-mvp-web
      helm:
        valueFiles:
          - $skool-mvp-gitops/environments/dev/skool-mvp-web/values.yaml
    - repoURL: https://github.com/hcuri/skool-mvp-gitops.git
      targetRevision: main
      ref: skool-mvp-gitops
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
