apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: skool-mvp-app
  namespace: argocd
  labels:
    app: skool-mvp-app
spec:
  project: default
  sources:
    - repoURL: https://github.com/hcuri/skool-mvp-app.git
      targetRevision: main
      path: charts/skool-mvp-app
      helm:
        valueFiles:
          - $skool-mvp-gitops/environments/dev/skool-mvp-app/values.yaml
    - repoURL: https://github.com/hcuri/skool-mvp-gitops.git
      targetRevision: main
      ref: skool-mvp-gitops
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
