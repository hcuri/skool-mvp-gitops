replicaCount: 2

image:
  repository: hcuri/skool-mvp-api
  tag: bd7798c
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 80

env:
  logLevel: info
  databaseSecretName: skool-mvp-db

servicemonitor:
  enabled: true # Prometheus (kube-prometheus-stack) scrapes /metrics via ServiceMonitor

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    memory: 128Mi
